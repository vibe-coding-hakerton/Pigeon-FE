# Pigeon 제품 기획서

> **작성일**: 2025-12-09
> **버전**: v0.6
> **상태**: Draft

### 변경 이력
| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v0.1 | 2025-12-09 | 초안 작성 (샘플 데이터 기반) |
| v0.2 | 2025-12-09 | Gmail API 연동으로 변경 |
| v0.3 | 2025-12-09 | 자동 동기화+분류 통합 (수동 버튼 제거) |
| v0.4 | 2025-12-09 | 인앱 토스트 → 브라우저 알림(Web Notification)으로 변경 |
| v0.5 | 2025-12-09 | "읽지 않은 메일함" 가상 폴더 추가, 읽음 처리 API 추가 |
| v0.6 | 2025-12-09 | 초기 동기화 UX 개선 (전체화면 대기 → 즉시 메인화면), 동기화 범위 6개월로 제한 |

---

## 1. 제품 개요

### 1.1 한 줄 정의
**Gmail과 연동하여 LLM이 이메일을 분석하고 자동으로 다계층 폴더에 분류해주는 AI 메일 폴더링 시스템**

### 1.2 문제 정의
| 문제 | 설명 |
|------|------|
| 이메일 과부하 | 매일 수십~수백 개의 이메일을 받지만 정리할 시간이 없음 |
| 단순 규칙의 한계 | 기존 필터(발신자, 키워드)는 맥락을 이해하지 못함 |
| 수동 분류 피로 | 폴더를 만들고 드래그&드롭으로 분류하는 것은 번거로움 |

### 1.3 해결책
- **Gmail API 연동**으로 실제 이메일을 가져옴
- AI가 이메일 **내용을 이해**하고 적절한 폴더 경로를 **자동 결정**
- 존재하지 않는 폴더는 **자동 생성**
- 사용자는 **트리 구조 UI**로 정리된 메일을 탐색
- 불필요한 메일 **삭제** 기능 제공

---

## 2. 타겟 사용자

### 2.1 페르소나
| 이름 | 역할 | 니즈 |
|------|------|------|
| **김개발** | 스타트업 개발자 | GitHub 알림, Jira, 회사 공지, 뉴스레터가 섞여서 중요 메일을 놓침 |
| **이매니저** | 프로젝트 매니저 | 프로젝트별로 메일을 정리하고 싶지만 시간이 없음 |
| **박프리** | 프리랜서 | 여러 클라이언트 메일을 프로젝트/고객별로 자동 분류하고 싶음 |

### 2.2 MVP 타겟
- Gmail 사용자 중 이메일 정리에 어려움을 겪는 **테크 종사자**
- 자동화 도구에 익숙한 **얼리어답터**

---

## 3. 핵심 기능 (MVP)

### 3.1 기능 목록

| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| F1 | Gmail 연동 | OAuth2 인증 후 Gmail API로 메일 조회 | P0 |
| F2 | **자동 동기화 + 분류** | 동기화와 AI 분류가 자동으로 연계 실행 (수동 버튼 없음) | P0 |
| F3 | 폴더 자동 생성 | 새로운 경로 감지 시 폴더 구조 자동 생성 | P0 |
| F4 | 트리 UI | 폴더 구조를 트리 형태로 시각화 | P0 |
| F5 | 메일 목록 | 선택한 폴더의 메일 목록 표시 | P0 |
| F6 | 메일 상세 | 메일 내용 상세 보기 | P0 |
| F7 | 새 메일 브라우저 알림 | Web Notification API로 새 메일 도착 알림 (권한 필요) | P0 |
| F8 | 읽지 않은 메일함 | 미읽음 메일만 모아보는 가상 폴더 (분류는 유지) | P0 |
| F9 | 메일 삭제 | Gmail에서 메일 삭제 (휴지통 이동) | P1 |
| F10 | 수동 폴더 이동 | 사용자가 메일을 다른 폴더로 수동 이동 | P1 |
| F11 | 메일 보내기 | Gmail로 메일 발송 | P2 |
| F12 | 분류 규칙 피드백 | 잘못된 분류 시 피드백으로 학습 | P2 (미구현) |

**핵심 원칙**: 사용자는 Gmail 로그인만 하면 끝. 이후 모든 동기화/분류는 자동.

### 3.2 기능 상세

#### F1: Gmail 연동 (OAuth2)
```
인증 흐름:
1. 사용자가 "Gmail 연동" 버튼 클릭
2. Google OAuth2 동의 화면으로 리다이렉트
3. 사용자가 권한 승인 (읽기, 삭제, 발송)
4. Access Token + Refresh Token 획득
5. 토큰을 서버에 저장 (세션/DB)

필요 권한 (Gmail API Scopes):
- gmail.readonly: 메일 조회
- gmail.modify: 메일 삭제 (휴지통 이동)
- gmail.send: 메일 발송 (P2)
```

#### F2: 메일 동기화 + 자동 분류 (통합)
```
핵심 원칙:
- 사용자는 Gmail 로그인만 하면 끝
- 동기화와 분류가 자동으로 연계되어 진행됨
- 수동 "분류하기" 버튼 없음
- 동기화 중에도 이미 가져온 메일은 즉시 조회 가능 (대기 화면 없음)

동기화 범위:
- 최초 동기화: 최근 6개월 이내 메일만 대상
- Gmail API 쿼리: after:{6개월전 timestamp}

동기화 + 분류 전략:

[초기 동기화 (최초 로그인)] - 즉시 메인 화면 진입
1. 로그인 완료 즉시 메인 화면으로 이동 (대기 화면 없음)
2. 백그라운드에서 동기화 + 분류 자동 시작
3. 배치 단위: 20개씩 클러스터링
   - 20개 동기화 완료 → 즉시 UI에 반영 + AI 분류 시작
   - 분류 진행 중에도 다음 20개 동기화 (병렬 처리)
4. 사용자는 동기화되는 대로 메일 조회 가능
5. 상태바에서 진행 상황 확인 (방해되지 않게)

[증분 동기화 (이후)]
1. 새 메일 감지 시 즉시 동기화
2. 새 메일 1개씩 실시간 분류 (또는 5개 모이면 배치)
3. 백그라운드에서 자동 처리

Gmail API 활용:
- users.messages.list: 메일 목록 조회 (after: 6개월 전)
- users.messages.get: 메일 상세 조회
- pageToken으로 페이지네이션 처리
- historyId로 증분 동기화

배치 사이즈 결정:
- 초기 동기화: 20개 단위 (LLM API 호출 최적화)
- 증분 동기화: 1~5개 단위 (실시간성 우선)
```

#### F3: AI 분류
```
입력: 메일 데이터 (제목, 발신자, 본문, 날짜)
출력: 폴더 경로 (예: "업무/프로젝트A/회의록")

분류 기준:
- 발신자 도메인 및 이름
- 메일 제목의 키워드
- 본문 내용의 맥락
- 기존 폴더 구조 참조

프롬프트 예시:
"다음 이메일을 분석하여 적절한 폴더 경로를 추천해주세요.
기존 폴더 구조: [트리 구조]
메일 정보: [제목, 발신자, 본문]
응답 형식: { "folderPath": "상위폴더/하위폴더/...", "reason": "분류 이유" }"
```

#### F4: 폴더 자동 생성
```
시나리오:
1. AI가 "업무/프로젝트B/계약서" 경로 추천
2. 시스템이 기존 폴더 구조 확인
3. "프로젝트B", "계약서" 폴더가 없으면 자동 생성
4. 메일을 해당 폴더에 배치

※ 폴더는 Pigeon 내부 개념 (Gmail 라벨과는 별개)
```

#### F5: 트리 UI
```
─────────────────────────
📬 읽지 않은 메일 (7)     ← 가상 폴더 (미읽음만 필터)
─────────────────────────
[전체 메일] (50)
├── [업무] (25)
│   ├── [프로젝트A] (10)  ● 2  ← 미읽음 표시
│   │   ├── 회의록 (5)
│   │   └── 보고서 (5)   ● 1
│   └── [프로젝트B] (15)
├── [개인] (10)
│   └── [쇼핑] (10)      ● 3
├── [알림] (5)
│   ├── [GitHub] (3)     ● 1
│   └── [Jira] (2)
└── [뉴스레터] (5)
    └── [기술] (5)

● = 읽지 않은 메일 있음 (숫자는 미읽음 개수)
```

#### F8: 읽지 않은 메일함 (가상 폴더)
```
개념:
- 실제 폴더가 아닌 필터 기반 가상 뷰
- 분류된 폴더는 그대로 유지됨
- isRead = false인 메일만 모아서 표시

동작:
1. 새 메일 도착 → 분류 진행 → 해당 폴더에 배치
2. 동시에 "읽지 않은 메일함"에도 표시됨
3. 사용자가 메일 클릭 (상세 보기) → isRead = true
4. "읽지 않은 메일함"에서 자동 제거
5. 분류된 폴더에는 그대로 유지

용도:
- 새 메일을 빠르게 확인할 수 있는 진입점
- 알림 클릭 시 이 폴더로 이동하면 편리
```

#### F9: 메일 삭제
```
삭제 방식:
1. 사용자가 메일 선택 후 "삭제" 클릭
2. Gmail API로 휴지통 이동 (users.messages.trash)
3. Pigeon DB에서도 해당 메일 삭제 표시
4. UI에서 즉시 제거

※ 영구 삭제는 지원하지 않음 (안전성)
```

#### F11: 메일 보내기 (P2)
```
기본 기능:
- 받는 사람, 제목, 본문 입력
- Gmail API로 발송 (users.messages.send)
- 발송 완료 후 "보낸편지함" 폴더에 표시

MVP에서는 후순위 (P2)
```

---

## 4. 사용자 시나리오

### 4.1 시나리오: 최초 접속 - Gmail 연동 및 자동 분류 (원스텝)

```
사용자 액션: Gmail 로그인만 하면 끝!

1. 사용자가 Pigeon 웹앱에 최초 접속
2. "Gmail 연동하기" 버튼 클릭
3. Google 로그인 및 권한 동의 화면
4. 권한 승인 후 Pigeon으로 리다이렉트
5. 브라우저 알림 권한 요청 (새 메일 알림용)
   - 승인 시: 백그라운드에서도 알림 수신 가능
   - 거부 시: 알림 없이 사용 (폴더 카운트만 업데이트)
6. 즉시 메인 화면으로 이동 (대기 화면 없음!)
7. 백그라운드에서 동기화 + 분류 자동 시작
   - 동기화 범위: 최근 6개월 메일
   - 20개씩 가져오면서 바로바로 UI에 표시
   - 분류도 병렬로 진행
8. 사용자는 동기화되는 대로 메일 탐색 가능
   - 이미 가져온 메일: 즉시 조회 가능
   - 아직 분류 안된 메일: "미분류" 상태로 표시
9. 하단 상태바에서 진행 상황 확인
   ─────────────────────────────────────────────
   🔄 동기화 중: 40/250 | 분류 중: 20/40 | 6개월 이내 메일
   ─────────────────────────────────────────────
```

### 4.2 시나리오: 재접속 - 새 메일 자동 처리

```
사용자 액션: 없음 (자동)

1. 사용자가 Pigeon에 재접속
2. 백그라운드에서 자동으로 새 메일 확인
3. 새 메일이 있으면:
   - 즉시 동기화 + AI 분류 (백그라운드)
   - 적절한 폴더에 자동 배치
4. 사용자는 이미 정리된 메일함 확인

※ 수동 "동기화" 버튼은 강제 새로고침용으로만 제공
```

### 4.3 시나리오: 백그라운드 자동 동기화 (앱 사용 중 또는 다른 탭)

```
사용자 액션: 없음 (자동)

1. 사용자가 Pigeon 사용 중 (또는 다른 탭에서 작업 중)
2. 주기적으로 새 메일 확인 (30초~1분 간격)
3. 새 메일 감지 시:
   - 브라우저 알림: "Pigeon - 새 메일 3개가 도착했습니다"
   - 백그라운드에서 분류 진행 (조용히)
4. 폴더 트리의 카운트 자동 업데이트
5. 알림 클릭 시 Pigeon 탭으로 이동
```

### 4.4 시나리오: 메일 상세 확인

```
1. 좌측 트리에서 폴더 선택
2. 중앙에 해당 폴더의 메일 목록 표시
3. 메일 항목 클릭
4. 우측에 메일 상세 내용 표시
   - 제목, 발신자, 날짜
   - 본문 내용
   - AI 분류 이유
   - [삭제] [다른 폴더로 이동] 버튼
```

### 4.5 시나리오: 메일 삭제

```
1. 메일 목록에서 삭제할 메일 선택 (체크박스)
2. "삭제" 버튼 클릭
3. 확인 다이얼로그: "선택한 N개 메일을 삭제하시겠습니까?"
4. 확인 시 Gmail 휴지통으로 이동
5. 메일 목록에서 즉시 제거
```

---

## 5. 화면 설계 (와이어프레임)

### 5.1 초기 화면 (미연동 상태)

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                        🕊️ Pigeon                                │
│                                                                 │
│              AI가 당신의 메일을 정리해드립니다                    │
│                                                                 │
│                  ┌─────────────────────┐                        │
│                  │  Gmail 연동하기  📧  │                        │
│                  └─────────────────────┘                        │
│                                                                 │
│              Gmail 계정과 연동하여 시작하세요                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 메인 레이아웃 (연동 후)

```
┌─────────────────────────────────────────────────────────────────┐
│  [🕊️ Pigeon]              user@gmail.com        [🔄] [설정] [로그아웃] │
├──────────────┬──────────────────────────────┬───────────────────┤
│              │                              │                   │
│   폴더 트리   │        메일 목록             │    메일 상세      │
│              │  ┌────────────────────────┐  │    (선택 시)      │
│ ▼ 전체 (50)  │  │ □ [전체 선택]     [삭제] │  │                   │
│   ▼ 업무 (25)│  └────────────────────────┘  │  From: 김팀장     │
│     프로젝트A │  □ 주간 회의록 - 김팀장 12/9 │  To: me           │
│     프로젝트B │  □ PR Review - GitHub  12/9 │  Date: 2025-12-09 │
│   ▼ 개인 (10)│  ■ 배송 완료 - 쿠팡    12/8 │  Subject: 주간 회의록│
│     쇼핑     │  □ 뉴스레터 - TechNews 12/8 │                   │
│   알림 (5)   │                              │  본문 내용...     │
│   뉴스레터(5)│                              │                   │
│              │                              │  ─────────────    │
│              │                              │  📁 분류: 업무/회의록│
│              │                              │  💡 이유: "회의 관련"│
│──────────────│──────────────────────────────│───────────────────│
│              │  ◀ 1 2 3 ... 10 ▶           │ [이동] [삭제]     │
├──────────────┴──────────────────────────────┴───────────────────┤
│  ✓ 동기화 완료 | 총 50개 메일 | 마지막 확인: 30초 전              │
└─────────────────────────────────────────────────────────────────┘

※ 수동 "분류하기" 버튼 없음 - 모든 분류는 자동으로 처리됨
※ [🔄] 버튼: 강제 새로고침 (새 메일 즉시 확인)
```

### 5.3 동기화 상태바 (메인 화면 하단)

```
※ 전체 화면 대기 없이, 메인 레이아웃 하단에 상태바로 표시

[동기화 진행 중일 때 - 하단 상태바]
┌─────────────────────────────────────────────────────────────────┐
│  🔄 동기화 중: 85/250 메일  │  🤖 분류 중: 60/85  │  6개월 이내  │
│  [=============================>                    ] 34%       │
└─────────────────────────────────────────────────────────────────┘

[동기화 완료 후 - 하단 상태바]
┌─────────────────────────────────────────────────────────────────┐
│  ✓ 동기화 완료 | 총 250개 메일 | 마지막 확인: 방금 전             │
└─────────────────────────────────────────────────────────────────┘

상태바 동작:
- 동기화 중: 진행률 표시, 클릭 시 상세 정보 팝오버
- 완료 후: 간단한 요약만 표시
- 에러 시: 빨간색으로 오류 메시지 + 재시도 버튼

장점:
- 사용자가 기다릴 필요 없이 바로 메일 탐색 가능
- 동기화되는 대로 폴더 트리와 메일 목록이 실시간 업데이트
- 진행 상황은 방해되지 않게 하단에서 확인
```

### 5.4 새 메일 브라우저 알림 (Web Notification)

```
┌─────────────────────────────────────────┐
│ 🕊️ Pigeon                               │
│                                         │
│ 새 메일 3개가 도착했습니다               │
│                                         │
│ pigeon.app                              │
└─────────────────────────────────────────┘

- OS 레벨 알림 (데스크톱 우상단 또는 알림 센터)
- 클릭 시 Pigeon 탭으로 포커스 이동
- 알림 권한 거부 시: 알림 없음, 폴더 카운트만 업데이트
```

### 5.5 알림 권한 요청 (최초 1회)

```
┌─────────────────────────────────────────┐
│  pigeon.app에서 알림을 표시하려고 합니다  │
│                                         │
│              [차단]  [허용]              │
└─────────────────────────────────────────┘

※ Gmail 연동 직후 자동으로 표시됨
※ 거부해도 서비스 이용에는 문제 없음
```

### 5.6 폴더 이동 모달

```
┌─────────────────────────────────────────┐
│  메일 이동                          [X] │
├─────────────────────────────────────────┤
│                                         │
│  "주간 회의록" 메일을 이동할 폴더 선택    │
│                                         │
│  ▼ 업무                                 │
│    ○ 프로젝트A                          │
│    ● 프로젝트B  ← 선택됨                │
│  ▼ 개인                                 │
│    ○ 쇼핑                               │
│  ○ 알림                                 │
│  ○ 뉴스레터                             │
│                                         │
│  [+ 새 폴더 만들기]                      │
│                                         │
├─────────────────────────────────────────┤
│              [취소]  [이동하기]          │
└─────────────────────────────────────────┘
```

---

## 6. 데이터 모델

### 6.1 Entity 정의

#### User (사용자)
```typescript
interface User {
  id: string;                    // UUID
  email: string;                 // Gmail 주소
  gmailAccessToken: string;      // Gmail API Access Token (암호화)
  gmailRefreshToken: string;     // Gmail API Refresh Token (암호화)
  tokenExpiresAt: Date;          // 토큰 만료 시간
  lastSyncAt: Date | null;       // 마지막 동기화 시간
  createdAt: Date;
  updatedAt: Date;
}
```

#### Mail (메일)
```typescript
interface Mail {
  id: string;                    // UUID (내부 ID)
  userId: string;                // User ID (FK)
  gmailId: string;               // Gmail Message ID (외부 ID)
  threadId: string;              // Gmail Thread ID
  subject: string;               // 메일 제목
  sender: string;                // 발신자 (이름 <이메일>)
  senderEmail: string;           // 발신자 이메일
  snippet: string;               // 메일 미리보기 (Gmail snippet)
  body: string;                  // 메일 본문 (HTML 또는 Plain)
  receivedAt: Date;              // 수신 일시
  labelIds: string[];            // Gmail 라벨 ID 목록
  folderId: string | null;       // Pigeon 폴더 ID (분류 결과)
  classificationReason: string | null;  // AI 분류 이유
  isClassified: boolean;         // 분류 완료 여부
  isRead: boolean;               // 읽음 여부 (기본값: false)
  isDeleted: boolean;            // 삭제 여부 (soft delete)
  createdAt: Date;
  updatedAt: Date;
}
```

#### Folder (폴더)
```typescript
interface Folder {
  id: string;                    // UUID
  userId: string;                // User ID (FK)
  name: string;                  // 폴더 이름
  parentId: string | null;       // 상위 폴더 ID (null이면 루트)
  path: string;                  // 전체 경로 (예: "업무/프로젝트A")
  depth: number;                 // 깊이 (0부터 시작)
  mailCount: number;             // 포함된 메일 수
  unreadCount: number;           // 읽지 않은 메일 수
  createdAt: Date;
  updatedAt: Date;
}
```

### 6.2 Gmail API 응답 매핑

```typescript
// Gmail API: users.messages.get 응답
interface GmailMessage {
  id: string;
  threadId: string;
  labelIds: string[];
  snippet: string;
  payload: {
    headers: Array<{ name: string; value: string }>;
    body: { data: string };
    parts?: Array<{ body: { data: string } }>;
  };
  internalDate: string;  // Unix timestamp (ms)
}

// 매핑 함수
function mapGmailToMail(gmail: GmailMessage): Partial<Mail> {
  const headers = gmail.payload.headers;
  return {
    gmailId: gmail.id,
    threadId: gmail.threadId,
    subject: getHeader(headers, 'Subject'),
    sender: getHeader(headers, 'From'),
    senderEmail: extractEmail(getHeader(headers, 'From')),
    snippet: gmail.snippet,
    body: decodeBody(gmail.payload),
    receivedAt: new Date(parseInt(gmail.internalDate)),
    labelIds: gmail.labelIds,
  };
}
```

---

## 7. API 명세 (초안)

### 7.1 인증 (Gmail OAuth2)

#### GET /api/auth/gmail
Gmail OAuth2 인증 시작
```
Redirect → Google OAuth2 동의 화면
```

#### GET /api/auth/gmail/callback
OAuth2 콜백 처리
```
Query: ?code=xxx&state=xxx
Response: Redirect to /dashboard (with session cookie)
```

#### POST /api/auth/logout
로그아웃
```json
// Response
{ "success": true }
```

#### GET /api/auth/me
현재 사용자 정보 조회
```json
// Response
{
  "id": "user-uuid",
  "email": "user@gmail.com",
  "lastSyncAt": "2025-12-09T10:00:00Z"
}
```

### 7.2 Gmail 동기화 + 자동 분류 (통합 API)

#### POST /api/sync/start
동기화 + 분류 시작 (초기 또는 강제 새로고침)
```json
// Request (optional)
{
  "fullSync": false       // true면 전체 재동기화 (기본: 증분 동기화)
}

// Response
{
  "jobId": "job-uuid",
  "status": "in_progress",
  "syncRange": "6_months"  // 초기 동기화 범위
}
```
> **Note**: 초기 동기화는 최근 6개월 메일만 대상. Gmail API 쿼리에 `after:{6개월전}` 적용.

#### GET /api/sync/status
동기화 + 분류 상태 조회 (SSE 또는 폴링)
```json
// Response
{
  "status": "in_progress",  // "idle" | "in_progress" | "completed" | "error"
  "sync": {
    "progress": 85,
    "total": 100,
    "currentMail": "주간 회의록 공유"
  },
  "classify": {
    "progress": 60,
    "total": 100,
    "currentMail": "GitHub: PR Review",
    "currentFolder": "알림/GitHub"
  },
  "newFolders": [
    { "path": "업무/프로젝트A", "mailCount": 12 },
    { "path": "알림/GitHub", "mailCount": 15 }
  ]
}
```

#### GET /api/sync/poll
새 메일 확인 (주기적 폴링 - 백그라운드 자동 호출)
```json
// Response
{
  "hasNewMails": true,
  "newMailCount": 3,
  "autoClassified": true,  // 이미 자동 분류됨
  "results": [
    { "mailId": "uuid-1", "subject": "PR Review", "folderPath": "알림/GitHub" }
  ]
}
```

### 7.3 메일 관련

#### GET /api/mails
메일 목록 조회
```json
// Query params: ?folderId=xxx&page=1&limit=20&unread=true
// unread=true: 읽지 않은 메일함 (가상 폴더)
// Response
{
  "mails": [
    {
      "id": "uuid-1",
      "gmailId": "gmail-msg-id",
      "subject": "주간 회의록",
      "sender": "김팀장 <teamlead@company.com>",
      "snippet": "안녕하세요, 이번 주...",
      "receivedAt": "2025-12-09T09:00:00Z",
      "folderId": "folder-uuid",
      "folderPath": "업무/프로젝트A",
      "isClassified": true,
      "isRead": false    // 읽음 여부 (목록에서 미읽음 표시용)
    }
  ],
  "total": 50,
  "page": 1,
  "totalPages": 3
}
```

#### GET /api/mails/:id
메일 상세 조회 (호출 시 자동으로 읽음 처리)
```json
// Response
{
  "id": "uuid-1",
  "gmailId": "gmail-msg-id",
  "subject": "주간 회의록",
  "sender": "김팀장 <teamlead@company.com>",
  "senderEmail": "teamlead@company.com",
  "body": "<html>...</html>",
  "receivedAt": "2025-12-09T09:00:00Z",
  "folderId": "folder-uuid",
  "folderPath": "업무/프로젝트A",
  "classificationReason": "회의록 관련 내용으로 판단",
  "isClassified": true,
  "isRead": true    // 조회 시 자동으로 true로 설정됨
}
```
> **동작**: 상세 조회 API 호출 시 해당 메일의 `isRead`가 `false`면 자동으로 `true`로 변경됨. 이에 따라 "읽지 않은 메일함"에서 제거되고, 해당 폴더의 `unreadCount`가 감소함.

#### DELETE /api/mails/:id
메일 삭제 (Gmail 휴지통으로 이동)
```json
// Response
{ "success": true, "trashedAt": "2025-12-09T12:00:00Z" }
```

#### DELETE /api/mails
메일 일괄 삭제
```json
// Request
{ "mailIds": ["uuid-1", "uuid-2", "uuid-3"] }

// Response
{ "success": true, "deletedCount": 3 }
```

#### PATCH /api/mails/:id/folder
메일 폴더 이동
```json
// Request
{ "folderId": "new-folder-uuid" }

// Response
{ "success": true, "folderId": "new-folder-uuid", "folderPath": "업무/프로젝트B" }
```

#### PATCH /api/mails/:id/read
메일 읽음 처리 (읽지 않은 메일함에서 제거)
```json
// Request (본문 없이 호출)

// Response
{
  "success": true,
  "mailId": "uuid-1",
  "isRead": true,
  "readAt": "2025-12-09T12:00:00Z"
}
```
> **Note**: 메일 상세 조회(GET /api/mails/:id) 호출 시 자동으로 읽음 처리되거나, 명시적으로 이 API를 호출할 수 있음.

### 7.4 폴더 관련

#### GET /api/folders
폴더 트리 조회
```json
// Response
{
  "folders": [
    {
      "id": "folder-1",
      "name": "업무",
      "parentId": null,
      "path": "업무",
      "depth": 0,
      "mailCount": 25,
      "unreadCount": 3,    // 읽지 않은 메일 수 (하위 폴더 미포함)
      "children": [
        {
          "id": "folder-2",
          "name": "프로젝트A",
          "parentId": "folder-1",
          "path": "업무/프로젝트A",
          "depth": 1,
          "mailCount": 10,
          "unreadCount": 2,
          "children": []
        }
      ]
    }
  ],
  "unclassifiedCount": 5,
  "totalUnreadCount": 7    // 전체 읽지 않은 메일 수 (읽지 않은 메일함 카운트용)
}
```

#### POST /api/folders
폴더 생성
```json
// Request
{
  "name": "프로젝트C",
  "parentId": "folder-1"  // null이면 루트
}

// Response
{
  "id": "folder-new",
  "name": "프로젝트C",
  "parentId": "folder-1",
  "path": "업무/프로젝트C",
  "depth": 1,
  "mailCount": 0
}
```

### 7.5 분류 관련 (내부 API - 자동 호출)

> **참고**: 분류는 동기화와 통합되어 자동 실행됨. 아래 API는 내부적으로 사용되거나 수동 재분류 시에만 사용.

#### POST /api/classify/single (내부용)
단일 메일 분류 (새 메일 실시간 분류 시 내부 호출)
```json
// Request
{ "mailId": "uuid-1234" }

// Response
{
  "mailId": "uuid-1234",
  "folderPath": "업무/프로젝트A",
  "folderId": "folder-uuid",
  "reason": "프로젝트A 관련 업무 메일로 판단",
  "newFoldersCreated": []
}
```

#### PATCH /api/mails/:id/reclassify
수동 재분류 요청 (사용자가 분류 결과에 불만족 시)
```json
// Request (선택적으로 힌트 제공 가능)
{
  "hint": "이건 업무 메일이 아니라 개인 메일이야"  // optional
}

// Response
{
  "mailId": "uuid-1234",
  "previousFolder": "업무/프로젝트A",
  "newFolder": "개인/기타",
  "reason": "사용자 힌트 반영하여 재분류"
}
```

### 7.6 메일 발송 (P2)

#### POST /api/mails/send
메일 발송
```json
// Request
{
  "to": ["recipient@example.com"],
  "cc": [],
  "subject": "메일 제목",
  "body": "메일 본문 내용",
  "isHtml": false
}

// Response
{
  "success": true,
  "gmailId": "sent-message-id",
  "threadId": "thread-id"
}
```

---

## 8. 시스템 아키텍처

### 8.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React)                        │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌────────────────┐  │
│  │ AuthPage  │ │FolderTree │ │ MailList  │ │  MailDetail    │  │
│  │(OAuth Flow)│ │ Component │ │ Component │ │  Component     │  │
│  └───────────┘ └───────────┘ └───────────┘ └────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/REST
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Backend (FastAPI)                          │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌────────────────┐  │
│  │ Auth API  │ │ Mail API  │ │Folder API │ │ Classify API   │  │
│  │(OAuth2)   │ │           │ │           │ │                │  │
│  └─────┬─────┘ └─────┬─────┘ └───────────┘ └───────┬────────┘  │
│        │             │                             │            │
│        ▼             ▼                             ▼            │
│  ┌───────────┐ ┌───────────┐                ┌────────────────┐  │
│  │  Gmail    │ │  Gmail    │                │  LLM Service   │  │
│  │  OAuth2   │ │  API      │                │  (OpenAI API)  │  │
│  │  Client   │ │  Client   │                └────────────────┘  │
│  └───────────┘ └───────────┘                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
       ┌───────────┐  ┌───────────┐  ┌───────────┐
       │  SQLite   │  │  Google   │  │  OpenAI   │
       │  Database │  │  Gmail    │  │  API      │
       └───────────┘  │  API      │  └───────────┘
                      └───────────┘
```

### 8.2 기술 스택 결정

| 영역 | 선택 | 이유 |
|------|------|------|
| **Frontend** | Next.js + TypeScript | SSR 지원, React 기반, 풍부한 에코시스템 |
| **Backend** | Django (Python) | 풀스택 프레임워크, ORM 강력, Admin 내장 |
| **Database** | PostgreSQL | JSONB, ArrayField, Django 통합 우수 |
| **LLM** | Gemini 2.5 Flash + LangChain | 비용 효율적, 모델 교체 용이 |
| **스타일링** | Tailwind CSS | 빠른 UI 개발 |
| **Gmail 연동** | Google Gmail API | 공식 API, OAuth2 지원 |
| **상태관리** | Zustand | 간단하고 가벼움 |

### 8.3 OAuth2 인증 흐름

```
┌────────┐      ┌────────┐      ┌────────┐      ┌────────┐
│ User   │      │Frontend│      │Backend │      │ Google │
└───┬────┘      └───┬────┘      └───┬────┘      └───┬────┘
    │               │               │               │
    │ 1. 연동 클릭   │               │               │
    │──────────────>│               │               │
    │               │ 2. /auth/gmail│               │
    │               │──────────────>│               │
    │               │               │ 3. OAuth URL  │
    │               │               │──────────────>│
    │               │    4. Redirect to Google      │
    │<──────────────────────────────────────────────│
    │               │               │               │
    │ 5. 권한 승인   │               │               │
    │──────────────────────────────────────────────>│
    │               │               │               │
    │               │    6. Callback with code      │
    │               │<──────────────────────────────│
    │               │               │               │
    │               │ 7. /auth/gmail/callback       │
    │               │──────────────>│               │
    │               │               │ 8. Token 교환 │
    │               │               │──────────────>│
    │               │               │<──────────────│
    │               │               │               │
    │               │ 9. Session 설정, Redirect     │
    │<──────────────────────────────│               │
    │               │               │               │
```

---

## 9. Epic & Story 구조 (Jira)

### 9.1 Epic 목록

| Epic | 설명 |
|------|------|
| **EPIC-1: 프로젝트 세팅** | 개발 환경 및 기본 구조 설정 |
| **EPIC-2: Gmail 연동** | OAuth2 인증 및 Gmail API 연동 |
| **EPIC-3: 백엔드 API** | REST API 서버 구축 |
| **EPIC-4: AI 분류 엔진** | LLM 기반 분류 로직 구현 |
| **EPIC-5: 프론트엔드 UI** | React 기반 웹 UI 구현 |
| **EPIC-6: 통합 및 테스트** | E2E 테스트 및 버그 수정 |

### 9.2 Story 분해

#### EPIC-1: 프로젝트 세팅
- [ ] Story: 백엔드 프로젝트 초기화 (Django + uv)
- [ ] Story: 프론트엔드 프로젝트 초기화 (React + Vite + TypeScript)
- [ ] Story: 데이터베이스 스키마 설계 및 마이그레이션
- [ ] Story: 개발 환경 설정 (환경변수, CORS 등)

#### EPIC-2: Gmail 연동 + 자동 분류 파이프라인 ⭐ 핵심
- [ ] Story: Google Cloud Console 프로젝트 설정
- [ ] Story: OAuth2 인증 플로우 구현 (백엔드)
- [ ] Story: Gmail API 클라이언트 구현
- [ ] Story: 초기 동기화 + 자동 분류 파이프라인 구현 (20개 배치)
- [ ] Story: 증분 동기화 + 실시간 분류 구현 (새 메일 감지)
- [ ] Story: 백그라운드 폴링 로직 구현 (30초~1분 간격)
- [ ] Story: 메일 삭제 기능 구현 (휴지통 이동)
- [ ] Story: 토큰 갱신 로직 구현

#### EPIC-3: 백엔드 API
- [ ] Story: 사용자 인증 API 구현 (세션 관리)
- [ ] Story: 메일 CRUD API 구현
- [ ] Story: 폴더 CRUD API 구현
- [ ] Story: 폴더 트리 조회 API 구현

#### EPIC-4: AI 분류 엔진
- [ ] Story: LLM API 연동 설정 (OpenAI)
- [ ] Story: 분류 프롬프트 설계 및 테스트
- [ ] Story: 분류 결과 파싱 및 폴더 자동 생성 로직
- [ ] Story: 배치 분류 최적화 (20개 단위 병렬 처리)
- [ ] Story: 수동 재분류 기능 구현 (힌트 기반)

#### EPIC-5: 프론트엔드 UI
- [ ] Story: 초기 화면 (Gmail 연동 페이지) 구현
- [ ] Story: 레이아웃 컴포넌트 구현
- [ ] Story: 동기화 상태바 컴포넌트 구현 (하단 고정, 실시간 업데이트)
- [ ] Story: 폴더 트리 컴포넌트 구현 (읽지 않은 메일 수 표시 포함)
- [ ] Story: 읽지 않은 메일함 구현 (가상 폴더)
- [ ] Story: 메일 목록 컴포넌트 구현 (미읽음 표시, 실시간 추가)
- [ ] Story: 메일 상세 컴포넌트 구현 (자동 읽음 처리)
- [ ] Story: 브라우저 알림 구현 (Web Notification API)
- [ ] Story: API 연동 및 상태 관리 (Zustand)
- [ ] Story: 백그라운드 폴링 훅 구현

#### EPIC-6: 통합 및 테스트
- [ ] Story: Playwright E2E 테스트 작성
- [ ] Story: 버그 수정 및 UI 개선
- [ ] Story: 데모 시나리오 준비

### 9.3 P2 (후순위)
- [ ] Story: 메일 발송 기능 구현 (Gmail send)
- [ ] Story: 메일 발송 UI 구현

---

## 10. MVP 범위 외 (향후 고려)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| Outlook 연동 | Microsoft Graph API | Post-MVP |
| 다중 계정 | 여러 Gmail 계정 관리 | Post-MVP |
| 분류 규칙 학습 | 사용자 피드백 기반 개선 | Post-MVP |
| 검색 기능 | 메일 내용 검색 | Post-MVP |
| 다크 모드 | UI 테마 | Post-MVP |
| 모바일 앱 | React Native | Post-MVP |

---

## 11. 오픈 이슈 / 결정 필요 사항

| # | 이슈 | 선택지 | 결정 |
|---|------|--------|------|
| 1 | LLM 제공자 | OpenAI / Claude / Gemini | **Gemini 2.5 Flash + LangChain** |
| 2 | 폴더 깊이 제한 | 제한 없음 / 최대 5단계 | **최대 5단계** |
| 3 | 분류 실패 처리 | "미분류" 폴더 / 재시도 | **1회 재시도 후 "미분류"** |
| 4 | 동기화 범위 (라벨) | 받은편지함만 / 전체 라벨 | 받은편지함만 (MVP) |
| 5 | 동기화 범위 (기간) | 전체 / 1년 / 6개월 / 3개월 | **6개월** |
| 6 | 메일 본문 저장 | Snippet만 / Full body / 하이브리드 | **하이브리드** (HTML 저장 + 이미지/첨부 참조) |
| 7 | Gmail API 할당량 | 일일 할당량 관리 전략 | **단순 Rate Limiting** |
| 8 | 초기 동기화 배치 크기 | 10개 / 20개 / 50개 | **20개** |
| 9 | 백그라운드 폴링 주기 | 30초 / 1분 / 3분 / 5분 | **3분** |
| 10 | 새 메일 분류 방식 | 즉시 1개씩 / 5개 모아서 배치 | **즉시 1개씩** |
| 11 | 초기 동기화 UX | 전체화면 대기 / 즉시 메인화면 | **즉시 메인화면** |
| 12 | Database | MySQL / PostgreSQL / MongoDB | **PostgreSQL** |
| 13 | Next.js 라우터 | App Router / Pages Router | **App Router** |

---

## 12. 보안 및 기술 요구사항

| 항목 | 대응 |
|------|------|
| **HTTPS 필수** | Web Notification API 사용을 위해 필수 |
| OAuth2 토큰 보관 | 서버에 암호화하여 저장, 클라이언트 노출 금지 |
| Refresh Token | Secure, HttpOnly 쿠키 또는 서버 세션 |
| 메일 본문 | 전송 시 HTTPS, 저장 시 암호화 고려 |
| CORS | 허용 도메인 제한 |
| Rate Limiting | Gmail API 할당량 준수 |
| 알림 권한 | 사용자 거부 시 graceful degradation (알림 없이 동작) |

---

*이 문서는 Day 1 기획 단계에서 작성되었으며, 개발 진행에 따라 업데이트됩니다.*
